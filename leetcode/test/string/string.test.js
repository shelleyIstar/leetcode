import revertByWord from '../../code/string/string'

// test('revertByWord:Let\'s take LeetCode contest', () => {
//     expect(revertByWord("Let's take LeetCode contest")).toBe("s'teL ekat edoCteeL tsetnoc")
// })

test('revertByWord:Let\'s take LeetCode contest', () => {
    expect(revertByWord('vector<string> split (string s, char delimiter)')).toBe('>gnirts<rotcev tilps gnirts( ,s rahc )retimiled')
})
